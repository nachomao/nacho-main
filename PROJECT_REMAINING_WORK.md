# 项目剩余工作

> 最后复核：2026-09-16
>
> 本文档是项目唯一的当前待办来源。README 用于说明使用方式，AGENTS.md 用于约束开发流程；其他历史计划、阶段验收记录和临时证据均不作为当前状态依据。

## P0：真实功能闭环

### 1. WebSSH 终端与文件管理

当前边界：

- 终端命令结果仍由前端 `runCommand()` 模拟。
- 文件目录仍来自硬编码 `MOCK_FS`。
- 上传、下载、删除及进度仍由本地 state／定时器模拟。

完成标准：

- 建立面板 → 控制服务端 → Agent 的真实命令与文件传输协议。
- 覆盖鉴权、权限控制、路径校验、大小限制、超时、取消、断线恢复和审计。
- 保留现有桌面／移动端布局、主题、动画和交互反馈。

### 2. AI Workspace

当前边界：

- 回答仍通过 `scenarios` 匹配预设内容。
- 工具步骤与执行结果仍由前端生成。
- 尚未接入真实模型、MCP、插件或 Agent 工具。

完成标准：

- 接入真实模型并支持流式响应。
- 工具调用必须具备用户确认、权限约束、失败恢复和审计。
- 增加会话持久化，并明确 MCP、插件与 Agent 工具的可信边界。

## P1：产品范围与生产能力

### 3. Linux Agent 范围决策

当前 Agent 以 Windows Service 和 Windows 平台能力为主。需要二选一并在产品、安装文档和界面中保持一致：

- 明确标记产品为 Windows-only；或
- 设计并实现 Linux Agent、安装方式、平台能力映射及对应测试。

### 4. 生产加固

- HTTPS/WSS 部署与证书生命周期管理。
- Agent 发布物代码签名及升级包签名验证。
- 评估证书固定策略及轮换方案。
- Panel API Key、Enrollment Key、设备 token 的生成、轮换、撤销与审计。
- 发布渠道、权限与回滚治理。

### 5. 升级能力扩展

- ARM64 发布物。
- MSI 安装／卸载形态。
- 可配置的定时自动升级。
- 历史版本选择与受控降级。
- Preview／Production 发布渠道。

### 6. 云端自动部署真机验收

- 在 Windows 本机面板对一台全新 Linux 服务器验证 SSH 主机指纹、root／sudo 登录、发布包传输、安装失败清理、健康检查及 8443 端口连通性。
- 验证自动生成的 Panel API Key 可被浏览器使用，既有服务不会被覆盖；再根据生产环境要求配置 HTTPS、访问控制和防火墙。

## 已确认的工程边界

- 云端服务入口提供“云端部署”和“云端对接”两项；后者保留原 API + Panel API Key 连接流程。云端自动部署只在 Windows 本机面板开放，限定同源 loopback 请求与 IPv4 目标，先展示 SSH 主机指纹供核对，再以密码认证通过 SFTP 上传经过 SHA-256 核验的发布制品和源码。仅对未安装的 Linux 服务器执行原有 `install.sh`，不覆盖已有配置和数据库；SSH 密码不持久化，面板密钥不写入安装日志。尚需在用户指定的真实 Linux 主机和 Windows 本机面板上执行完整 SSH 安装、sudo、外部端口与浏览器连接验收；仅凭源码测试和预览界面不能宣称远程真机部署已验收。
- 控制服务端支持 Windows 本机、WSL2 和独立 Linux 服务器三种部署模式；三者共用 `server/` 源码与 API 协议，但进程、配置、SQLite、端口和验收状态相互独立。面板与 Agent 一次只连接一个选定服务端；Windows 本机任务不自动启动 WSL，多模式同机并存时使用不同端口。
- Windows 本机控制服务已支持从首次引导与设置页执行环境检查、安装、启动／停止／重启、可回滚修复、端口迁移、HKCU 用户登录自启、loopback／局域网监听切换和彻底卸载；安装期间通过同源 NDJSON 响应实时显示 PowerShell、npm 依赖安装和构建输出，完成后再切换到运行状态、访问范围和监听端口等参数。Windows 管理脚本保留 UTF-8 BOM、显式按 UTF-8 读取项目清单并以 UTF-8 输出错误，兼容 Windows PowerShell 5.1。运行环境检测通过隐藏、非交互的 PowerShell 进程执行，优先复用面板当前 `process.execPath` 与 Node.js 同目录 npm；检测 `npm.cmd` 时由 `node.exe` 直接执行 `npm-cli.js`，命中后停止继续枚举，避免状态轮询周期性创建可见的 `cmd.exe` 窗口。系统 PATH、`where.exe` 与常见安装目录仅作为回退，并在执行 npm 后立即保存退出码，不因后续管道命令覆盖 `$LASTEXITCODE` 或一次处理器架构探测失败而误判。安装和启动会先做真实端口占用预检，状态脚本通过一次 `netstat` 快照核对受管 PID、监听端口和占用进程，避免全量 `Get-NetTCPConnection` 阻塞管理 API；状态探测受独立短超时约束，失败时显示“运行异常”并禁用依赖准确进程状态的操作，不再误报“已停止”。受管 Node 子进程显式载入 `server/.env`，避免继承面板 `PORT`／`NODE_ENV` 导致监听错误。安装和不完整状态修复会在下载、构建或改写配置前检查端口占用，安装后续步骤失败时自动停止受管进程并清理本次新建的配置、构建、依赖、运行时、自启和防火墙状态。仍缺少 Node.js 22+ 或 npm 时，安装／修复流程才下载 Node.js 官方 LTS 便携包，校验官方 SHA-256 后放入项目受管目录，无需 App Installer、管理员权限或修改系统 PATH，下载失败且 winget 可用时再回退到系统安装。局域网模式只创建 `Private + LocalSubnet + 指定 TCP 端口` 的入站规则，Agent 安装链接使用可达局域网地址，卸载会删除本地 SQLite、日志、依赖、受管运行环境和构建产物但保留源码。管理 API 仅接受面板同源 JSON 请求，状态判定同时校验受管 PID、配置端口和健康响应。Windows 服务通过独立 Node 启动器脱离安装请求的 stdout／stderr 管道后再把运行日志写入 `.nacho-local`；`Start` 管理动作以 PowerShell 进程的 `exit` 而非可能被后代句柄延迟的 `close` 作为完成信号，随即进入健康检查，避免安装停在“正在启动”。WSL2 与独立 Linux 服务器通过 `install.sh` 和 systemd 管理，不由 Windows 本机管理卡片模拟操作。
- 首次引导的服务方式与登录方式菜单在展开／收起时使用 `focus({ preventScroll: true })` 交接焦点，避免尚在展开的裁剪容器隐式滚动后回正；卡片扩张、模糊缩放渐显与标题错峰动画保持不变。本机控制服务卡片内部的“安装设置 → 实时输出 → 运行状态”也复用同一组高度、模糊、缩放和错峰渐显过渡，安装完成时保留退出中的日志内容，避免阶段切换瞬闪；运行环境补齐与局域网管理员授权提示在条件变化时同步使用高度、间距、模糊和纵向位移过渡，外层窗口不再瞬间跳高。
- 首次引导头像仅保留用户提供的“抱心猫娘”“趴趴猫娘”“猫帽猫娘”三款本地 PNG 预设，资源文件和 `avatarId` 已同步改为 `heart`／`lounge`／`hood`；旧预设标识在读取时迁移为默认的“抱心猫娘”。称呼确认、头像确认和上传入口继续复用中性按钮样式，头像动画、自定义上传与面板本机设置保存链路不变。
- 面板的 `turbopack.root` 固定为 `nacho-panel/`，避免开发服务器把同级 `server/artifacts/` 发布制品纳入观察范围并在 Windows 上造成进程与内存耗尽；根目录代理脚本和 Vercel 构建仍通过 `pnpm --dir nacho-panel` 进入面板工作目录。
- 通知中心会把服务端最近的系统业务日志物化为可读、可清空和可稍后提醒的通知；日志级别、来源、详情���关联客户端会随通知保留，信息日志使用“提示”样式展示。
- `tasks` 已使用服务端真实 API 完成读取、CRUD、启停和任务下发，不再列为缺口。
- `health` 已使用服务端真实 API 完成发现项、采集、下载、重分析和重采集，不再列为缺口。
- `plugins` 的目录读取、CRUD 与启停状态已由服务端持久化；插件包下载、导入和批量安装仍未形成真实包传输与 Agent 执行闭环。
- WebSSH 与 AI Workspace 仍是前端演示边界，不得在文档或界面中宣称已完成真实执行。
- 服务端源码测试按当前任务选择的 Windows、WSL2 或独立 Linux 环境执行；涉及 Linux 部署链路时使用 Linux／WSL 原生依赖，不要使用历史测试数量替代当前验证。

## 完成与更新规则

1. 开始任务前先按当前源码、测试与运行态复核本清单。
2. 完成项必须具备相关层级的实现、测试、错误处理、安全约束和真实链路验证。
3. 涉及 UI 时必须保留既有视觉效果与动画，并核对桌面及 `390x844` 窄屏。
4. 每完成或发现一项，直接更新本文档；不要再创建新的 `NEXT_TASK`、`REMAINING_WORK` 或阶段计划文档。
5. 测试数量、版本、哈希、服务状态和部署结果只记录当次重新取得的证据。
